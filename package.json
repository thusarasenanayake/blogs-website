{
  "name": "blogs",
  "version": "1.0.0",
  "description": "Website for displaying blog posts loaded from json file/server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thusara Senanayake",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "json-server": "^0.16.3"
  }
}
